# Bus-Ticket-Booking-System
A simple Java Swing desktop app using JDBC and MySQL (XAMPP-ready) to search bus routes, add bookings, and cancel them. Features include route search by source, destination, and date, booking creation with customer details, seat count auto-update, viewing bookings, and sample SQL with 10 buses and 10 routes.
